# Core dependencies
tabulate>=0.9.0

# Monitoring and metrics
prometheus-client>=0.19.0
psutil>=5.9.0  # For memory monitoring in prometheus exporter

# Database (using built-in sqlite3, but documenting for clarity)
# sqlite3 - built into Python standard library

# Testing dependencies
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# Optional dependencies for future enhancements
# celery>=5.3.0  # For async task execution
# redis>=5.0.0   # For Celery backend
# pydantic>=2.0.0  # For configuration validation